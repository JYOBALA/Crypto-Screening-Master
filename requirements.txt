pandas>=2.0
numpy>=1.24
requests>=2.31
pyarrow>=14.0
openpyxl>=3.1
# Output ide_trade ke Google Sheets (OUTPUT_TARGET=gsheet|both, daily_run.py) --
# pengecualian yang disengaja thd aturan "hindari dependensi baru" (CLAUDE.md),
# sama seperti openpyxl di atas. Auth service account, bukan OAuth user.
gspread>=6.0
google-auth>=2.30
